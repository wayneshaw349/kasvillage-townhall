const fs=require('fs');let s=fs.readFileSync('content_validator.ts','utf8');s=`export function validateContentText(text: string): string | null {\n  if (!text) return null;\n  const blocked = ['child trafficking','human trafficking','sell children','buy children','sell weapons','buy weapons','sell drugs','buy drugs','sell knives','buy knives'];\n  const lower = text.toLowerCase();\n  for (const phrase of blocked) { if (lower.includes(phrase)) return 'Content rejected: prohibited phrase detected'; }\n  return null;\n}`;fs.writeFileSync('content_validator.ts',s);console.log('done');
