const fs=require('fs');let s=fs.readFileSync('NeighborAgreement.tsx','utf8');s=s.replace("import { IOUBalanceSheetShare } from './IOUBalanceSheetShare';","import IOUBalanceSheetModal from './IOUBalanceSheetShare';");s=s.replace("{showBalanceSheet && <IOUBalanceSheetShare />}","{showBalanceSheet && <IOUBalanceSheetModal visible={showBalanceSheet} onClose={() => setShowBalanceSheet(false)} />}");fs.writeFileSync('NeighborAgreement.tsx',s);console.log('done');
