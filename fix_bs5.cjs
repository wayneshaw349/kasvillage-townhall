const fs=require('fs');let s=fs.readFileSync('NeighborAgreement.tsx','utf8');s=s.replace(/<Text style=\{\{ color: "#1d4ed8".*View Balance Sheet.*<\/TouchableOpacity>/s,(m)=>m+'\n            {showBalanceSheet && <IOUBalanceSheetShare />}');if(!s.includes("import { IOUBalanceSheetShare }"))s=s.replace("import React","import { IOUBalanceSheetShare } from './IOUBalanceSheetShare';\nimport React");fs.writeFileSync('NeighborAgreement.tsx',s);console.log('done');
