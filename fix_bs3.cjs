const fs=require('fs');let s=fs.readFileSync('NeighborAgreement.tsx','utf8');s=s.replace("const [showBalanceSheet, setShowBalanceSheet] = useState(false);","const [showBalanceSheet, setShowBalanceSheet] = useState(false);\n  const IOUSheet = showBalanceSheet ? require('./IOUBalanceSheetShare').IOUBalanceSheetShare : null;");s=s.replace(/return \(\s*<View/,"return (\n    <View");s=s.replace("{ setShowBalanceSheet(true); }","{ setShowBalanceSheet(!showBalanceSheet); }");fs.writeFileSync('NeighborAgreement.tsx',s);console.log('done');
