const fs=require('fs');let s=fs.readFileSync('PhoneProofScreen.tsx','utf8');s=s.replace(/if \(input\.startsWith\('APT-'\)\) \{[^}]*\} catch \{\} \}/s,`if (input.startsWith('APT-')) { try { const r = await fetch('https://arweave.net/graphql', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ query: '{ transactions(tags: [{ name: "App-Name", values: ["KasVillage"] }, { name: "KV-APT", values: ["' + input + '"] }], first: 1, sort: HEIGHT_DESC) { edges { node { tags { name value } } } } }' }) }); const j = await r.json(); const t = j?.data?.transactions?.edges?.[0]?.node?.tags || []; pk = t.find((x: any) => x.name === 'KV-Pubkey')?.value || ''; } catch {} }`);fs.writeFileSync('PhoneProofScreen.tsx',s);console.log('done');
