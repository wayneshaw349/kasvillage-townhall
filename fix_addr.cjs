const fs=require('fs');let s=fs.readFileSync('IOUBalanceSheetShare.tsx','utf8');s=s.replace(/const addr = await SecureStore\.getItemAsync\('kv_kaspa_address'\);\r?\n\s*if \(!addr\)/,"const addr = await SecureStore.getItemAsync('kv_kaspa_address') || await SecureStore.getItemAsync('kaspa_address');\n                  if (!addr)");fs.writeFileSync('IOUBalanceSheetShare.tsx',s);console.log('done');
