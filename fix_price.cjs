const fs=require('fs');let s=fs.readFileSync('ReceiveScreen.tsx','utf8');s=s.replace("const { price: kasPrice } = useKaspaPrice();","const { usdPerKas } = useKaspaPrice();");s=s.replace(/kasPrice\?\.price \? \(/,"usdPerKas ? (");s=s.replace("(Number(balance) / 1e8 * kasPrice.price)","(Number(balance) / 1e8 * usdPerKas)");fs.writeFileSync('ReceiveScreen.tsx',s);console.log('done');
