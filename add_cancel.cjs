const fs=require('fs');let s=fs.readFileSync('IOUBalanceSheetShare.tsx','utf8');
// State for pending IOU
s=s.replace("const [showActive, setShowActive] = useState(false);","const [showActive, setShowActive] = useState(false);\n  const [pendingIOU, setPendingIOU] = useState<any>(null);");
// Load + auto-expire on mount
s=s.replace("console.log('[IOU] resolved pubkey:', !!pubkey);","console.log('[IOU] resolved pubkey:', !!pubkey);\n      try {\n        const pj = await SecureStore.getItemAsync('kv_pending_iou');\n        if (pj) {\n          const p = JSON.parse(pj);\n          if (Date.now() - p.created > p.expiresMs) {\n            await releaseIOU(p.iouId);\n            await SecureStore.deleteItemAsync('kv_pending_iou');\n            setPendingIOU(null);\n            console.log('[IOU] pending IOU expired, hold released');\n          } else { setPendingIOU(p); }\n        }\n      } catch {}");
// Pending IOU card with Cancel button — insert before Active IOUs toggle
s=s.replace("{/* Active IOUs */}","{pendingIOU && (\n              <View style={{ marginTop: 12, backgroundColor: '#3a2a1a', borderRadius: 10, padding: 14, borderWidth: 1, borderColor: '#D4AF37' }}>\n                <Text style={{ color: '#D4AF37', fontWeight: 'bold', fontSize: 13 }}>Pending IOU — awaiting signed acceptance</Text>\n                <Text style={{ color: '#fff', fontSize: 14, marginTop: 4 }}>{pendingIOU.amount} KAS held</Text>\n                <Text style={{ color: '#888', fontSize: 11, marginTop: 2 }}>Auto-releases {new Date(pendingIOU.created + pendingIOU.expiresMs).toLocaleString()}</Text>\n                <TouchableOpacity onPress={async () => { try { await releaseIOU(pendingIOU.iouId); await SecureStore.deleteItemAsync('kv_pending_iou'); setPendingIOU(null); Alert.alert('Released', 'IOU hold cancelled, funds freed'); } catch(e:any) { Alert.alert('Error', e.message); } }} style={{ marginTop: 10, backgroundColor: '#e74c3c20', padding: 10, borderRadius: 8, alignItems: 'center', borderWidth: 1, borderColor: '#e74c3c' }}>\n                  <Text style={{ color: '#e74c3c', fontWeight: 'bold', fontSize: 13 }}>Cancel IOU Hold</Text>\n                </TouchableOpacity>\n              </View>\n            )}\n            {/* Active IOUs */}");
fs.writeFileSync('IOUBalanceSheetShare.tsx',s);console.log('done');
