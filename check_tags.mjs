fetch('https://arweave.net/graphql',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({query:'{ transactions(tags: [{ name: "App-Name", values: ["KasVillage"] }], first: 5, sort: HEIGHT_DESC) { edges { node { id tags { name value } } } } }'})}).then(r=>r.json()).then(j=>j.data.transactions.edges.forEach(e=>{console.log('---');e.node.tags.forEach(t=>console.log(t.name+': '+t.value));}));
