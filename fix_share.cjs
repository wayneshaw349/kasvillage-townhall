const fs=require('fs');let s=fs.readFileSync('IOUBalanceSheetShare.tsx','utf8');s=s.replace("const p = await createProposal('iou', parseFloat(proposalAmount), proposalDesc); await shareProposal(p);","const p = await createProposal('iou', parseFloat(proposalAmount), proposalDesc);\n                  if ('error' in p) throw new Error(p.error);\n                  await shareProposal(p.encoded);");fs.writeFileSync('IOUBalanceSheetShare.tsx',s);console.log('done');
