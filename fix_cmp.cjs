const fs=require('fs');let s=fs.readFileSync('IOUBalanceSheetShare.tsx','utf8');s=s.replace(/if \(spendable\.total < amtSompi\) throw new Error\('Insufficient free balance: ' \+ \(Number\(spendable\.total\)\/1e8\)\.toFixed\(4\) \+ ' KAS available'\);/,"if (BigInt(spendable.total) < amtSompi) throw new Error('Insufficient free balance: ' + (Number(spendable.total)/1e8).toFixed(4) + ' KAS available');");fs.writeFileSync('IOUBalanceSheetShare.tsx',s);console.log('done');
