const fs=require('fs');let s=fs.readFileSync('IOUBalanceSheetShare.tsx','utf8');s=s.replace("await allocateForIOU(addr, String(Math.floor(parseFloat(proposalAmount) * 1e8)), 'iou-' + Date.now());","const alloc = await allocateForIOU(addr, amtSompi, 'iou-' + Date.now());\n                  if (!alloc.success) throw new Error(alloc.error || 'Allocation failed');");fs.writeFileSync('IOUBalanceSheetShare.tsx',s);console.log('done');
